import React, { ReactNode, Component } from "react";
import { View } from "native-base";

export class ResultScreenView extends Component<any, any> {

    public render(): ReactNode {
        return (
            <View></View>
        );
    }
}